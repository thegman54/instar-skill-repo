name: repo
display_name: Repository
description: Clone, read, write, search, commit, push, and manage git repositories
category: Repository
icon: folder
color: orange
version: 2.0.0

credentials:
  - key: GITHUB_TOKEN
    description: GitHub Personal Access Token for clone/push operations

grant_metadata:
  - key: allowed_repos
    description: Comma-separated list of allowed GitHub repos (e.g. "owner/repo1,owner/repo2") or "*" for any
